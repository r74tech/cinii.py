from setuptools import setup, find_packages

setup(
  name='cinii',
  version='0.1.0',
  license='MIT',
  description='This is a wrapper for the CiNii API.',
  author='RTa-technology',
  packages=["cinii"],
)